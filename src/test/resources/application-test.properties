# Dummy IMAP config so the Spring context can build MailService without real
# secrets. The "test" profile also disables MailDemoRunner, so nothing connects.
mail.imap.username=test@example.com
mail.imap.password=dummy
